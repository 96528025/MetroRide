# Single-node KinD cluster used only for ephemeral deployment validation inside
# a GitHub Actions runner (or on a developer machine). It is created, used and
# deleted within one job; it is not hosting and it creates nothing outside the
# machine it runs on.
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
